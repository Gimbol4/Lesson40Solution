// Задание 3: Вызовите функцию `print_сhar_and_string` в `main`.

#include <iostream>
using namespace std;

char print_сhar_and_string(char symbol, string text)
{
  cout << symbol << ": " << text;
}

int main()
{
    char symbol;
    string text;
    char print_char_and_string(symbol, text);
    return 0;
}